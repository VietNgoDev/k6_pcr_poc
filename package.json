{
  "name": "k6-pcr-poc",
  "version": "1.0.0",
  "description": "Lean, production-grade k6 and TypeScript load-testing PoC",
  "scripts": {
    "build": "tsc --noEmit && node -e \"require('esbuild').buildSync({ entryPoints: ['src/export.ts', 'src/mock-server.ts'], outdir: 'dist', bundle: true, platform: 'node', format: 'cjs', target: 'node16' })\"",
    "mock-server": "node dist/mock-server.js"
  },
  "devDependencies": {
    "@types/k6": "^0.54.2",
    "@types/node": "^22.19.19",
    "esbuild": "^0.25.0",
    "typescript": "^5.4.5"
  }
}
